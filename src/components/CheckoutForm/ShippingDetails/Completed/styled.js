import styled from 'styled-components'

const Completed = styled.div`
  display: grid;
  grid-template-columns: 1fr 1fr;
  text-align: left;
  font-size: 14px;

  label {
    display: block;
    margin: 2px 0;
  }
`

export { Completed }
